services:
  python:
    build: .
    container_name: node-dev
    environment:
      - PULSE_SERVER=host.docker.internal
    volumes:
      - ./:/app
    working_dir: /app
    stdin_open: true
    tty: true